{
  "packages": [
    "pydantic>=2.0",
    "pytest"
  ],
  "shared_types": {
    "ScaffoldFilePath": "enum(package.json, tsconfig.json, tsconfig.node.json, vite.config.ts, tailwind.config.ts, postcss.config.mjs, vercel.json, index.html, vitest.setup.ts, src/main.tsx, src/index.css, src/App.tsx)",
    "ProjectManifest": "struct(projectRoot: str, files: list, projectName: str, devServerPort: int)",
    "DependencyEntry": "struct(packageName: str, versionRange: str, isDev: bool)",
    "RequiredDependencies": "struct(dependencies: list, devDependencies: list)",
    "ScaffoldResult": "struct(manifest: ProjectManifest, filesWritten: int, hasJsFiles: bool, hasPyFiles: bool)",
    "ScaffoldOptions": "struct(outputDir: str, overwrite: bool)",
    "ScaffoldError": "enum(DIRECTORY_NOT_FOUND, FILE_ALREADY_EXISTS, WRITE_PERMISSION_DENIED, INVALID_OUTPUT_DIR)",
    "PageComponentKey": "enum(Home, Cartographer, Constrain, Ledger, Pact, Advocate, Arbiter, Baton, Sentinel, Chronicler, Stigmergy, Apprentice, Kindex)",
    "RouteEntry": "struct(path: string, label: string, componentKey: PageComponentKey)",
    "RoutePath": "struct(value: str)",
    "RouteLabel": "struct(value: str)",
    "RouteManifest": "list",
    "PageComponentMap": "struct(Home: ReactComponentType, Step0Cartographer: ReactComponentType, Step1aConstrain: ReactComponentType, Step1bLedger: ReactComponentType, Step2aPact: ReactComponentType, Step2bAdvocate: ReactComponentType, Step3Arbiter: ReactComponentType, Step4Baton: ReactComponentType, Step5aSentinel: ReactComponentType, Step5bChronicler: ReactComponentType, Step5cStigmergy: ReactComponentType, Step6Apprentice: ReactComponentType, Step7Kindex: ReactComponentType)",
    "ReactComponentType": "primitive",
    "AppProps": "struct",
    "string": "struct",
    "CalloutType": "enum(gotcha, warning, tip)",
    "ReactNode": "primitive",
    "CalloutBoxProps": "struct(type: CalloutType, title: string, children: ReactNode)",
    "CodeBlockProps": "struct(code: string, language: string, title: string)",
    "VersionBadgeProps": "struct(version: string, label: string)",
    "VideoEmbedProps": "struct(url: string, title: string)",
    "SidebarProps": "struct(routes: RouteManifest)",
    "PageLayoutProps": "struct(routes: RouteManifest, children: ReactNode)"
  },
  "conventions": [
    "Language: typescript",
    "Testing: vitest",
    "Type annotations on all public functions",
    "Prefer composition over inheritance",
    "All functions must have at least one test",
    "Tests must be runnable without external services",
    "No task is done until its contract tests pass",
    "Prefer stdlib over third-party libraries",
    "Keep files under 300 lines",
    "Named exports: every type, interface, function, and constant MUST be a named export",
    "React components: export BOTH as named AND default (e.g. `export function Foo() {}` + `export default Foo`)",
    "Every .tsx file must include `import React from 'react'` at the top",
    "Tests run with vitest + @testing-library/react in jsdom environment",
    "Do NOT use `export default` as the only export \u2014 always pair with a named export",
    "NEVER infer key strings from component names or natural language \u2014 they may differ from the contract value",
    "ALWAYS read the key strings from: (1) the TypeScript interface definition for the map, (2) the contract test's test data, or (3) the dependency component's exported manifest/enum",
    "A key that \"looks right\" by name may not match the contract \u2014 always verify against the source of truth",
    "ALWAYS use `import.meta.env.VITE_X` directly \u2014 never `(import.meta as any).env?.VITE_X`",
    "The `as any` cast bypasses vitest's vi.stubEnv: stubs return undefined instead of the stubbed value",
    "Correct: `const url = import.meta.env.VITE_CONVEX_URL;`",
    "Wrong: `const url = (import.meta as any).env?.VITE_CONVEX_URL;`"
  ],
  "validators": [
    "custom(value === false)",
    "length(length >= 1)",
    "length(min=1,max=128)",
    "length(min=1,max=64)",
    "range(value == 13)",
    "range(value == 4000)",
    "regex(^/[a-z0-9-]*$)"
  ],
  "tools": [],
  "mocks": {}
}