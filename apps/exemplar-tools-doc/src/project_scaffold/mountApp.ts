const PACT_KEY = "PACT:d97abb:project_scaffold";

export function mountApp(): void {
  // This is a placeholder — the actual mount logic lives in main.tsx
  // which is the side-effect entry point.
  throw new Error('mountApp is not meant to be called directly. Import main.tsx instead.');
}
