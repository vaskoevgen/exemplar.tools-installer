const PACT_KEY = "PACT:d97abb:project_scaffold";
console.debug(PACT_KEY, "postcss.config loaded");

const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
