const PACT_KEY = "PACT:d97abb:project_scaffold";

export function mountApp(): void {
  console.debug(PACT_KEY, "mountApp:called");
  // This is a reference implementation for the required export.
  // The actual side-effect entry point is main.tsx.
}
