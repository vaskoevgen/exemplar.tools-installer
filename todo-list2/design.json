{
  "project_id": "todo-list2",
  "title": "Design: todo-list2",
  "summary": "",
  "decomposition_tree": {
    "root_id": "root",
    "nodes": {
      "root": {
        "component_id": "root",
        "name": "Main",
        "description": "# Task\n\nBuild a personal task management web app with a React frontend, Express/Node.js backend, and PostgreSQL database.\n\n## Requirements\n\n### Backend (Express/Node.js)\n\n- REST API with the following endpoints:\n  - `POST /tasks` — create a task (title required, returns 400 if title is empty/missing)\n  - `GET /tasks` — list all tasks\n  - `GET /tasks/:id` — get a single task (returns 404 if not found)\n  - `PATCH /tasks/:id` — update title, completed, priority, or due_date (returns 404 if not found)\n  - `DELETE /tasks/:id` — delete a task (returns 404 if not found)\n- Task fields: `id` (uuid), `title` (varchar, NOT NULL), `completed` (boolean, default false), `priority` (integer, default 1), `due_date` (date, nullable), `created_at` (timestamptz, NOT NULL)\n- Proper HTTP status codes: 200, 201, 400, 404\n- Backend is authoritative — no client-side caching of state\n\n### Database (PostgreSQL)\n\n- Single `tasks` table with the fields above\n- Normalized schema appropriate for a single-user app (no unnecessary joins)\n- Index on `priority` and `due_date` for future query patterns\n- At least one schema migration (e.g. adding `priority` column after initial schema)\n\n### Frontend (React)\n\n- List all tasks\n- Create a new task (title input)\n- Mark a task as complete/incomplete (toggle)\n- Delete a task\n- Display priority and due date if set\n\n## Learning Goals\n\n- Practice REST API design with correct HTTP verbs and status codes\n- Practice PostgreSQL schema design and migrations\n- Practice connecting a React frontend to a REST backend",
        "depth": 0,
        "parent_id": "",
        "children": [],
        "contract": {
          "component_id": "root",
          "name": "Main",
          "description": "Root interface contract for a personal task management web app. Defines canonical types (frozen dataclasses), REST endpoint contracts, and pure validation functions for a React/Express/PostgreSQL stack. All types use Python 3.12+ stdlib dataclasses (frozen=True, slots=True, kw_only=True). JSON Merge Patch semantics (RFC 7396) for PATCH: absent key = unchanged, explicit null = clear field. No envelope wrappers — response bodies are bare types. Serialization: snake_case keys, lowercase hyphenated UUIDs, ISO 8601 timestamps with Z suffix, YYYY-MM-DD dates, integer priority.",
          "version": 1,
          "types": [
            {
              "name": "TaskUUID",
              "kind": "primitive",
              "fields": [
                {
                  "name": "value",
                  "type_ref": "str",
                  "required": true,
                  "default": "",
                  "description": "Lowercase hyphenated UUID v4 string.",
                  "validators": [
                    {
                      "kind": "regex",
                      "expression": "^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$",
                      "error_message": "Must be a valid lowercase hyphenated UUID v4."
                    }
                  ]
                }
              ],
              "item_type": "",
              "variants": [],
              "inner_types": [],
              "description": "A UUID v4 string in lowercase hyphenated form (8-4-4-4-12). Used as the primary key for task records.",
              "owner_component": ""
            },
            {
              "name": "TaskTitle",
              "kind": "primitive",
              "fields": [
                {
                  "name": "value",
                  "type_ref": "str",
                  "required": true,
                  "default": "",
                  "description": "The task title string, stripped of leading/trailing whitespace.",
                  "validators": [
                    {
                      "kind": "length",
                      "expression": "1..500",
                      "error_message": "Title must be between 1 and 500 characters after stripping whitespace."
                    },
                    {
                      "kind": "custom",
                      "expression": "value == value.strip()",
                      "error_message": "Title must be pre-stripped of leading/trailing whitespace."
                    }
                  ]
                }
              ],
              "item_type": "",
              "variants": [],
              "inner_types": [],
              "description": "A non-empty, whitespace-stripped task title. Max 500 characters after stripping.",
              "owner_component": ""
            },
            {
              "name": "Priority",
              "kind": "primitive",
              "fields": [],
              "item_type": "",
              "variants": [],
              "inner_types": [],
              "description": "An integer representing task priority (1 = default/lowest, higher = more urgent).",
              "owner_component": "root"
            },
            {
              "name": "DateString",
              "kind": "primitive",
              "fields": [],
              "item_type": "",
              "variants": [],
              "inner_types": [],
              "description": "An ISO 8601 date string in YYYY-MM-DD format (no time component).",
              "owner_component": "root"
            },
            {
              "name": "TimestampTZ",
              "kind": "primitive",
              "fields": [],
              "item_type": "",
              "variants": [],
              "inner_types": [],
              "description": "An ISO 8601 timestamp string with timezone (e.g. 2024-01-15T10:30:00.000Z).",
              "owner_component": "root"
            },
            {
              "name": "OptionalDateString",
              "kind": "optional",
              "fields": [],
              "item_type": "DateString",
              "variants": [],
              "inner_types": [],
              "description": "A nullable date string; null indicates no due date is set.",
              "owner_component": "root"
            },
            {
              "name": "HttpStatusCode",
              "kind": "enum",
              "fields": [],
              "item_type": "",
              "variants": [
                "200",
                "201",
                "400",
                "404"
              ],
              "inner_types": [],
              "description": "The set of HTTP status codes used across the API for consistent response handling.",
              "owner_component": "root"
            },
            {
              "name": "Task",
              "kind": "struct",
              "fields": [
                {
                  "name": "id",
                  "type_ref": "UUID",
                  "required": true,
                  "default": "",
                  "description": "Unique identifier for the task, generated server-side as a v4 UUID.",
                  "validators": []
                },
                {
                  "name": "title",
                  "type_ref": "string",
                  "required": true,
                  "default": "",
                  "description": "The task title; must be a non-empty string.",
                  "validators": [
                    {
                      "kind": "length",
                      "expression": "min=1",
                      "error_message": "Title must not be empty."
                    }
                  ]
                },
                {
                  "name": "completed",
                  "type_ref": "boolean",
                  "required": true,
                  "default": "false",
                  "description": "Whether the task has been marked as complete.",
                  "validators": []
                },
                {
                  "name": "priority",
                  "type_ref": "Priority",
                  "required": true,
                  "default": "1",
                  "description": "Task priority level; defaults to 1.",
                  "validators": [
                    {
                      "kind": "range",
                      "expression": "min=1",
                      "error_message": "Priority must be a positive integer."
                    }
                  ]
                },
                {
                  "name": "due_date",
                  "type_ref": "OptionalDateString",
                  "required": false,
                  "default": "null",
                  "description": "Optional due date for the task in YYYY-MM-DD format.",
                  "validators": []
                },
                {
                  "name": "created_at",
                  "type_ref": "TimestampTZ",
                  "required": true,
                  "default": "",
                  "description": "Server-generated timestamp of when the task was created.",
                  "validators": []
                }
              ],
              "item_type": "",
              "variants": [],
              "inner_types": [],
              "description": "The canonical task entity shared across database, backend API, and frontend display.",
              "owner_component": "root"
            },
            {
              "name": "TaskList",
              "kind": "list",
              "fields": [],
              "item_type": "Task",
              "variants": [],
              "inner_types": [],
              "description": "An ordered list of Task objects returned by the GET /tasks endpoint.",
              "owner_component": "root"
            },
            {
              "name": "CreateTaskRequest",
              "kind": "struct",
              "fields": [
                {
                  "name": "title",
                  "type_ref": "string",
                  "required": true,
                  "default": "",
                  "description": "The title for the new task; must be a non-empty string.",
                  "validators": [
                    {
                      "kind": "length",
                      "expression": "min=1",
                      "error_message": "Title must not be empty."
                    }
                  ]
                },
                {
                  "name": "priority",
                  "type_ref": "Priority",
                  "required": false,
                  "default": "1",
                  "description": "Optional priority override; defaults to 1 if omitted.",
                  "validators": [
                    {
                      "kind": "range",
                      "expression": "min=1",
                      "error_message": "Priority must be a positive integer."
                    }
                  ]
                },
                {
                  "name": "due_date",
                  "type_ref": "OptionalDateString",
                  "required": false,
                  "default": "null",
                  "description": "Optional due date in YYYY-MM-DD format.",
                  "validators": []
                }
              ],
              "item_type": "",
              "variants": [],
              "inner_types": [],
              "description": "Request body for POST /tasks — only title is required; other fields are optional overrides.",
              "owner_component": "root"
            },
            {
              "name": "UpdateTaskRequest",
              "kind": "struct",
              "fields": [
                {
                  "name": "title",
                  "type_ref": "string",
                  "required": false,
                  "default": "",
                  "description": "New title for the task; must be non-empty if provided.",
                  "validators": [
                    {
                      "kind": "length",
                      "expression": "min=1",
                      "error_message": "Title must not be empty."
                    }
                  ]
                },
                {
                  "name": "completed",
                  "type_ref": "boolean",
                  "required": false,
                  "default": "",
                  "description": "New completion status for the task.",
                  "validators": []
                },
                {
                  "name": "priority",
                  "type_ref": "Priority",
                  "required": false,
                  "default": "",
                  "description": "New priority level for the task.",
                  "validators": [
                    {
                      "kind": "range",
                      "expression": "min=1",
                      "error_message": "Priority must be a positive integer."
                    }
                  ]
                },
                {
                  "name": "due_date",
                  "type_ref": "OptionalDateString",
                  "required": false,
                  "default": "",
                  "description": "New due date; set to null to clear, or a YYYY-MM-DD string to set.",
                  "validators": []
                }
              ],
              "item_type": "",
              "variants": [],
              "inner_types": [],
              "description": "Request body for PATCH /tasks/:id — all fields are optional; only provided fields are updated.",
              "owner_component": "root"
            },
            {
              "name": "ApiErrorResponse",
              "kind": "struct",
              "fields": [
                {
                  "name": "status",
                  "type_ref": "HttpStatusCode",
                  "required": true,
                  "default": "",
                  "description": "The HTTP status code of the error (e.g. 400, 404).",
                  "validators": []
                },
                {
                  "name": "error",
                  "type_ref": "string",
                  "required": true,
                  "default": "",
                  "description": "A human-readable error message describing what went wrong.",
                  "validators": []
                }
              ],
              "item_type": "",
              "variants": [],
              "inner_types": [],
              "description": "Standard error response body returned by the API for 400 and 404 responses.",
              "owner_component": "root"
            },
            {
              "name": "EndpointContract",
              "kind": "struct",
              "fields": [
                {
                  "name": "method",
                  "type_ref": "str",
                  "required": true,
                  "default": "",
                  "description": "HTTP method: GET, POST, PATCH, DELETE.",
                  "validators": [
                    {
                      "kind": "custom",
                      "expression": "method in ('GET', 'POST', 'PATCH', 'DELETE')",
                      "error_message": "Method must be one of GET, POST, PATCH, DELETE."
                    }
                  ]
                },
                {
                  "name": "path",
                  "type_ref": "str",
                  "required": true,
                  "default": "",
                  "description": "URL path pattern, e.g. '/tasks' or '/tasks/:id'.",
                  "validators": [
                    {
                      "kind": "regex",
                      "expression": "^/tasks(/:id)?$",
                      "error_message": "Path must be '/tasks' or '/tasks/:id'."
                    }
                  ]
                },
                {
                  "name": "request_type",
                  "type_ref": "str",
                  "required": true,
                  "default": "",
                  "description": "Name of the request body type, or 'None' if no body.",
                  "validators": []
                },
                {
                  "name": "success_status",
                  "type_ref": "HttpStatusCode",
                  "required": true,
                  "default": "",
                  "description": "HTTP status code on success.",
                  "validators": []
                },
                {
                  "name": "success_response_type",
                  "type_ref": "str",
                  "required": true,
                  "default": "",
                  "description": "Name of the response type on success.",
                  "validators": []
                },
                {
                  "name": "error_branches",
                  "type_ref": "list",
                  "required": true,
                  "default": "",
                  "description": "List of (HttpStatusCode, ApiErrorResponse) error branch specifications.",
                  "validators": []
                }
              ],
              "item_type": "",
              "variants": [],
              "inner_types": [],
              "description": "Describes a single REST endpoint contract: HTTP method, path pattern, request type, and mapping of status codes to response types. Used in contracts.py to define exhaustive endpoint specifications.",
              "owner_component": ""
            },
            {
              "name": "ValidationResult",
              "kind": "union",
              "fields": [],
              "item_type": "",
              "variants": [],
              "inner_types": [
                "CreateTaskRequest",
                "UpdateTaskRequest",
                "str"
              ],
              "description": "Return type for validation functions. Either a validated request object (CreateTaskRequest or UpdateTaskRequest with validated fields) or an error string describing the first validation failure.",
              "owner_component": ""
            },
            {
              "name": "Sentinel",
              "kind": "enum",
              "fields": [],
              "item_type": "",
              "variants": [
                "UNSET"
              ],
              "inner_types": [],
              "description": "Sentinel enum with a single UNSET member. Used in UpdateTaskRequest to distinguish absent fields from explicitly-set fields (including null). Needed for due_date where null means 'clear the due date' vs absent means 'do not change'.",
              "owner_component": ""
            },
            {
              "name": "UUID",
              "kind": "primitive",
              "fields": [],
              "item_type": "",
              "variants": [],
              "inner_types": [],
              "description": "A v4 UUID string uniquely identifying a task.",
              "owner_component": "root"
            },
            {
              "name": "boolean",
              "kind": "struct",
              "fields": [],
              "item_type": "",
              "variants": [],
              "inner_types": [],
              "description": "Auto-stubbed type — referenced but not defined in contract 'root'",
              "owner_component": ""
            },
            {
              "name": "string",
              "kind": "struct",
              "fields": [],
              "item_type": "",
              "variants": [],
              "inner_types": [],
              "description": "Auto-stubbed type — referenced but not defined in contract 'root'",
              "owner_component": ""
            }
          ],
          "functions": [
            {
              "name": "validate_create_task",
              "description": "Pure validation function for CreateTaskRequest. Strips whitespace from title, rejects empty titles, validates priority >= 1, validates due_date format (YYYY-MM-DD) if provided. Returns either a validated CreateTaskRequest with stripped title or an error string describing the first validation failure. Defined in validators.py.",
              "inputs": [
                {
                  "name": "request",
                  "type_ref": "CreateTaskRequest",
                  "required": true,
                  "default": "",
                  "description": "Raw create task request from the client, before validation.",
                  "validators": []
                }
              ],
              "output_type": "ValidationResult",
              "error_cases": [
                {
                  "name": "empty_title",
                  "condition": "request.title is None, not a string, or empty/whitespace-only after stripping",
                  "error_type": "str",
                  "error_data": {
                    "example": "Title is required and must not be empty."
                  }
                },
                {
                  "name": "title_too_long",
                  "condition": "request.title.strip() exceeds 500 characters",
                  "error_type": "str",
                  "error_data": {
                    "example": "Title must not exceed 500 characters."
                  }
                },
                {
                  "name": "invalid_priority",
                  "condition": "request.priority is not an integer or is < 1",
                  "error_type": "str",
                  "error_data": {
                    "example": "Priority must be a positive integer >= 1."
                  }
                },
                {
                  "name": "invalid_due_date",
                  "condition": "request.due_date is provided but does not match YYYY-MM-DD or is not a valid calendar date",
                  "error_type": "str",
                  "error_data": {
                    "example": "Due date must be a valid date in YYYY-MM-DD format."
                  }
                }
              ],
              "preconditions": [
                "request is a CreateTaskRequest instance"
              ],
              "postconditions": [
                "On success: returned CreateTaskRequest has title == request.title.strip() and len(title) >= 1 and len(title) <= 500",
                "On success: returned CreateTaskRequest has priority >= 1",
                "On success: if due_date is not null, it is a valid YYYY-MM-DD date string",
                "On error: returned str is a non-empty human-readable error message"
              ],
              "idempotent": true,
              "is_async": false,
              "side_effects": [],
              "structured_side_effects": [
                {
                  "kind": "none",
                  "target": "",
                  "description": "Pure validation function with no side effects."
                }
              ],
              "performance_budget": null
            },
            {
              "name": "validate_update_task",
              "description": "Pure validation function for UpdateTaskRequest. For each field that is not UNSET: strips and validates title (non-empty, <= 500 chars), validates priority >= 1, validates due_date format (YYYY-MM-DD) or null. Returns either a validated UpdateTaskRequest or an error string. At least one field must be non-UNSET. Defined in validators.py.",
              "inputs": [
                {
                  "name": "request",
                  "type_ref": "UpdateTaskRequest",
                  "required": true,
                  "default": "",
                  "description": "Raw update task request with UNSET sentinels for absent fields.",
                  "validators": []
                }
              ],
              "output_type": "ValidationResult",
              "error_cases": [
                {
                  "name": "no_fields_provided",
                  "condition": "All fields in the request are UNSET (empty patch body)",
                  "error_type": "str",
                  "error_data": {
                    "example": "At least one field must be provided for update."
                  }
                },
                {
                  "name": "empty_title",
                  "condition": "title is provided but is empty/whitespace-only after stripping",
                  "error_type": "str",
                  "error_data": {
                    "example": "Title must not be empty."
                  }
                },
                {
                  "name": "title_too_long",
                  "condition": "title is provided and exceeds 500 characters after stripping",
                  "error_type": "str",
                  "error_data": {
                    "example": "Title must not exceed 500 characters."
                  }
                },
                {
                  "name": "invalid_priority",
                  "condition": "priority is provided but is not an integer or is < 1",
                  "error_type": "str",
                  "error_data": {
                    "example": "Priority must be a positive integer >= 1."
                  }
                },
                {
                  "name": "invalid_due_date",
                  "condition": "due_date is provided (not UNSET, not null) but does not match YYYY-MM-DD or is not a valid calendar date",
                  "error_type": "str",
                  "error_data": {
                    "example": "Due date must be a valid date in YYYY-MM-DD format."
                  }
                },
                {
                  "name": "invalid_completed",
                  "condition": "completed is provided but is not a boolean",
                  "error_type": "str",
                  "error_data": {
                    "example": "Completed must be a boolean."
                  }
                }
              ],
              "preconditions": [
                "request is an UpdateTaskRequest instance"
              ],
              "postconditions": [
                "On success: at least one field in the returned UpdateTaskRequest is not UNSET",
                "On success: if title is not UNSET, it equals request.title.strip() and len >= 1 and len <= 500",
                "On success: if priority is not UNSET, it is >= 1",
                "On success: if due_date is not UNSET and not None, it is a valid YYYY-MM-DD date string",
                "On success: if completed is not UNSET, it is a bool",
                "On error: returned str is a non-empty human-readable error message"
              ],
              "idempotent": true,
              "is_async": false,
              "side_effects": [],
              "structured_side_effects": [
                {
                  "kind": "none",
                  "target": "",
                  "description": "Pure validation function with no side effects."
                }
              ],
              "performance_budget": null
            },
            {
              "name": "create_task",
              "description": "POST /tasks — Creates a new task. Validates the request body, generates a UUID v4 id and created_at timestamp server-side, inserts into PostgreSQL, and returns the created Task with HTTP 201. Returns HTTP 400 with ApiErrorResponse if validation fails.",
              "inputs": [
                {
                  "name": "request",
                  "type_ref": "CreateTaskRequest",
                  "required": true,
                  "default": "",
                  "description": "The create task request body parsed from JSON.",
                  "validators": []
                }
              ],
              "output_type": "Task",
              "error_cases": [
                {
                  "name": "validation_error",
                  "condition": "validate_create_task returns an error string (empty title, invalid priority, invalid due_date)",
                  "error_type": "ApiErrorResponse",
                  "error_data": {
                    "status": "400",
                    "error": "VALIDATION_ERROR"
                  }
                }
              ],
              "preconditions": [
                "request has been deserialized from a valid JSON object"
              ],
              "postconditions": [
                "On 201: returned Task.id is a valid UUID v4",
                "On 201: returned Task.title == request.title.strip()",
                "On 201: returned Task.completed == false",
                "On 201: returned Task.priority == request.priority (or 1 if not provided)",
                "On 201: returned Task.due_date == validated request.due_date (or null if not provided)",
                "On 201: returned Task.created_at is a valid UTC timestamp at or after the time the request was received",
                "On 201: a row with the returned Task's id exists in the tasks table"
              ],
              "idempotent": false,
              "is_async": true,
              "side_effects": [
                "Inserts a row into the PostgreSQL tasks table"
              ],
              "structured_side_effects": [
                {
                  "kind": "mutates_state",
                  "target": "postgresql.tasks",
                  "description": "INSERT INTO tasks — creates a new task row with server-generated id and created_at."
                }
              ],
              "performance_budget": {
                "p95_latency_ms": 200,
                "max_memory_mb": null,
                "complexity": "O(1)"
              }
            },
            {
              "name": "list_tasks",
              "description": "GET /tasks — Returns all tasks as a JSON array (TaskList). No pagination in this single-user app. Returns HTTP 200 with an empty array if no tasks exist.",
              "inputs": [],
              "output_type": "TaskList",
              "error_cases": [],
              "preconditions": [],
              "postconditions": [
                "Returned list contains exactly the set of tasks currently in the tasks table",
                "Each element in the returned list is a valid Task",
                "HTTP status is always 200"
              ],
              "idempotent": true,
              "is_async": true,
              "side_effects": [
                "Reads all rows from the PostgreSQL tasks table"
              ],
              "structured_side_effects": [
                {
                  "kind": "mutates_state",
                  "target": "postgresql.tasks",
                  "description": "SELECT * FROM tasks — reads all task rows. No mutation, but classified as state access."
                }
              ],
              "performance_budget": {
                "p95_latency_ms": 300,
                "max_memory_mb": null,
                "complexity": "O(n)"
              }
            },
            {
              "name": "get_task",
              "description": "GET /tasks/:id — Returns a single task by UUID. Returns HTTP 200 with the Task if found, HTTP 404 with ApiErrorResponse if not found.",
              "inputs": [
                {
                  "name": "task_id",
                  "type_ref": "TaskUUID",
                  "required": true,
                  "default": "",
                  "description": "The UUID of the task to retrieve, extracted from the URL path parameter :id.",
                  "validators": []
                }
              ],
              "output_type": "Task",
              "error_cases": [
                {
                  "name": "task_not_found",
                  "condition": "No row exists in the tasks table with the given task_id",
                  "error_type": "ApiErrorResponse",
                  "error_data": {
                    "status": "404",
                    "error": "NOT_FOUND",
                    "message": "Task not found."
                  }
                }
              ],
              "preconditions": [
                "task_id is a valid UUID v4 string"
              ],
              "postconditions": [
                "On 200: returned Task.id == task_id",
                "On 200: returned Task matches the current state of the row in the tasks table",
                "On 404: returned ApiErrorResponse.error == 'NOT_FOUND'"
              ],
              "idempotent": true,
              "is_async": true,
              "side_effects": [
                "Reads a row from the PostgreSQL tasks table"
              ],
              "structured_side_effects": [
                {
                  "kind": "mutates_state",
                  "target": "postgresql.tasks",
                  "description": "SELECT FROM tasks WHERE id = :id — reads a single task row by primary key."
                }
              ],
              "performance_budget": {
                "p95_latency_ms": 100,
                "max_memory_mb": null,
                "complexity": "O(1)"
              }
            },
            {
              "name": "update_task",
              "description": "PATCH /tasks/:id — Updates a task using JSON Merge Patch semantics (RFC 7396). Only fields present in the request body are updated; absent fields are unchanged. Explicit null on due_date clears it. Returns HTTP 200 with the full updated Task. Returns HTTP 404 if not found, HTTP 400 if validation fails.",
              "inputs": [
                {
                  "name": "task_id",
                  "type_ref": "TaskUUID",
                  "required": true,
                  "default": "",
                  "description": "The UUID of the task to update, extracted from the URL path parameter :id.",
                  "validators": []
                },
                {
                  "name": "request",
                  "type_ref": "UpdateTaskRequest",
                  "required": true,
                  "default": "",
                  "description": "The update request body with UNSET sentinels for absent fields.",
                  "validators": []
                }
              ],
              "output_type": "Task",
              "error_cases": [
                {
                  "name": "task_not_found",
                  "condition": "No row exists in the tasks table with the given task_id",
                  "error_type": "ApiErrorResponse",
                  "error_data": {
                    "status": "404",
                    "error": "NOT_FOUND",
                    "message": "Task not found."
                  }
                },
                {
                  "name": "validation_error",
                  "condition": "validate_update_task returns an error string (empty title, invalid priority, invalid due_date, no fields, invalid completed)",
                  "error_type": "ApiErrorResponse",
                  "error_data": {
                    "status": "400",
                    "error": "VALIDATION_ERROR"
                  }
                }
              ],
              "preconditions": [
                "task_id is a valid UUID v4 string",
                "request has been deserialized from a valid JSON object with UNSET sentinels applied for absent keys"
              ],
              "postconditions": [
                "On 200: returned Task.id == task_id",
                "On 200: for each field in request that is not UNSET, the returned Task reflects the new value",
                "On 200: for each field in request that is UNSET, the returned Task retains the previous value",
                "On 200: returned Task.created_at is unchanged from the original",
                "On 200: the tasks table row has been updated to match the returned Task",
                "On 404: returned ApiErrorResponse.error == 'NOT_FOUND'",
                "On 400: returned ApiErrorResponse.error == 'VALIDATION_ERROR'"
              ],
              "idempotent": true,
              "is_async": true,
              "side_effects": [
                "Updates a row in the PostgreSQL tasks table"
              ],
              "structured_side_effects": [
                {
                  "kind": "mutates_state",
                  "target": "postgresql.tasks",
                  "description": "UPDATE tasks SET ... WHERE id = :id — updates specified fields on the task row."
                }
              ],
              "performance_budget": {
                "p95_latency_ms": 200,
                "max_memory_mb": null,
                "complexity": "O(1)"
              }
            },
            {
              "name": "delete_task",
              "description": "DELETE /tasks/:id — Deletes a task by UUID. Returns HTTP 200 with the deleted Task object (snapshot before deletion). Returns HTTP 404 with ApiErrorResponse if not found.",
              "inputs": [
                {
                  "name": "task_id",
                  "type_ref": "TaskUUID",
                  "required": true,
                  "default": "",
                  "description": "The UUID of the task to delete, extracted from the URL path parameter :id.",
                  "validators": []
                }
              ],
              "output_type": "Task",
              "error_cases": [
                {
                  "name": "task_not_found",
                  "condition": "No row exists in the tasks table with the given task_id",
                  "error_type": "ApiErrorResponse",
                  "error_data": {
                    "status": "404",
                    "error": "NOT_FOUND",
                    "message": "Task not found."
                  }
                }
              ],
              "preconditions": [
                "task_id is a valid UUID v4 string"
              ],
              "postconditions": [
                "On 200: returned Task.id == task_id",
                "On 200: returned Task is the state of the task immediately before deletion",
                "On 200: no row with the given task_id exists in the tasks table after the call",
                "On 404: returned ApiErrorResponse.error == 'NOT_FOUND'",
                "On 404: the tasks table is unchanged"
              ],
              "idempotent": false,
              "is_async": true,
              "side_effects": [
                "Deletes a row from the PostgreSQL tasks table"
              ],
              "structured_side_effects": [
                {
                  "kind": "mutates_state",
                  "target": "postgresql.tasks",
                  "description": "DELETE FROM tasks WHERE id = :id RETURNING * — removes the task row and returns the deleted record."
                }
              ],
              "performance_budget": {
                "p95_latency_ms": 200,
                "max_memory_mb": null,
                "complexity": "O(1)"
              }
            },
            {
              "name": "get_endpoint_contracts",
              "description": "Returns the exhaustive list of EndpointContract definitions for all 5 REST endpoints. Used in contracts.py for endpoint contract specification and in test_contracts.py for exhaustiveness assertions. Pure function, returns a static list.",
              "inputs": [],
              "output_type": "list",
              "error_cases": [],
              "preconditions": [],
              "postconditions": [
                "Returned list contains exactly 5 EndpointContract objects",
                "Endpoints covered: POST /tasks, GET /tasks, GET /tasks/:id, PATCH /tasks/:id, DELETE /tasks/:id",
                "Every HttpStatusCode variant used in the API appears in at least one contract",
                "Every error branch maps to ApiErrorResponse"
              ],
              "idempotent": true,
              "is_async": false,
              "side_effects": [],
              "structured_side_effects": [
                {
                  "kind": "none",
                  "target": "",
                  "description": "Pure function returning static endpoint contract definitions."
                }
              ],
              "performance_budget": null
            },
            {
              "name": "task_to_dict",
              "description": "Serializes a Task to a JSON-compatible dict with snake_case keys. UUID as lowercase hyphenated string, timestamps as ISO 8601 with Z, dates as YYYY-MM-DD, priority as integer, due_date as string or null. No envelope wrapper. Defined in types.py for serialization round-trip support.",
              "inputs": [
                {
                  "name": "task",
                  "type_ref": "Task",
                  "required": true,
                  "default": "",
                  "description": "The Task instance to serialize.",
                  "validators": []
                }
              ],
              "output_type": "dict",
              "error_cases": [],
              "preconditions": [
                "task is a valid Task instance"
              ],
              "postconditions": [
                "Returned dict has exactly the keys: id, title, completed, priority, due_date, created_at",
                "All keys are snake_case strings",
                "dict['id'] is a lowercase hyphenated UUID string",
                "dict['created_at'] is an ISO 8601 string ending in Z",
                "dict['due_date'] is a YYYY-MM-DD string or None",
                "dict['priority'] is an int >= 1",
                "dict['completed'] is a bool"
              ],
              "idempotent": true,
              "is_async": false,
              "side_effects": [],
              "structured_side_effects": [
                {
                  "kind": "none",
                  "target": "",
                  "description": "Pure serialization function with no side effects."
                }
              ],
              "performance_budget": null
            },
            {
              "name": "task_from_dict",
              "description": "Deserializes a dict (e.g., from JSON or a database row mapping) into a Task instance. Validates all fields. Defined in types.py for serialization round-trip support.",
              "inputs": [
                {
                  "name": "data",
                  "type_ref": "dict",
                  "required": true,
                  "default": "",
                  "description": "A dict with snake_case keys matching the Task schema.",
                  "validators": []
                }
              ],
              "output_type": "Task",
              "error_cases": [
                {
                  "name": "missing_required_field",
                  "condition": "A required field (id, title, created_at) is missing from the dict",
                  "error_type": "str",
                  "error_data": {
                    "example": "Missing required field: title"
                  }
                },
                {
                  "name": "invalid_field_value",
                  "condition": "A field value fails its type validator (e.g., invalid UUID, empty title, priority < 1)",
                  "error_type": "str",
                  "error_data": {
                    "example": "Invalid value for field 'priority': must be >= 1"
                  }
                }
              ],
              "preconditions": [
                "data is a dict with string keys"
              ],
              "postconditions": [
                "Returned Task has all fields populated from the dict",
                "task_to_dict(task_from_dict(data)) == data (round-trip identity for well-formed input)"
              ],
              "idempotent": true,
              "is_async": false,
              "side_effects": [],
              "structured_side_effects": [
                {
                  "kind": "none",
                  "target": "",
                  "description": "Pure deserialization function with no side effects."
                }
              ],
              "performance_budget": null
            }
          ],
          "dependencies": [],
          "dependency_contracts": {},
          "invariants": [
            "All Task instances are immutable (frozen dataclasses). Mutation requires creating a new instance.",
            "Task.id is a UUID v4, generated server-side, and is unique across all tasks.",
            "Task.title is never empty or whitespace-only after validation. Max 500 characters.",
            "Task.priority is always >= 1.",
            "Task.created_at is always a valid UTC timestamp and is immutable after creation.",
            "Task.due_date is either a valid YYYY-MM-DD date string or None.",
            "All JSON serialization uses snake_case keys with no envelope wrapper.",
            "UUID strings are always lowercase hyphenated (8-4-4-4-12).",
            "Timestamps are always ISO 8601 with Z suffix (UTC).",
            "PATCH semantics follow RFC 7396 JSON Merge Patch: absent = unchanged, null = clear (for nullable fields).",
            "The Sentinel.UNSET value is used only in UpdateTaskRequest to distinguish absent from null; it never appears in serialized JSON or in Task instances.",
            "validate_create_task and validate_update_task are pure functions with no side effects.",
            "HTTP 201 is used only for POST /tasks success. All other successes use HTTP 200.",
            "HTTP 400 is returned only for validation errors. HTTP 404 is returned only when a task_id does not match any existing row.",
            "DELETE /tasks/:id returns the deleted Task snapshot (HTTP 200), not an empty body.",
            "The tasks table has indexes on priority and due_date columns for future query optimization.",
            "The backend is the single source of truth — no client-side caching of authoritative state."
          ],
          "requires": [],
          "processing_register": "pragmatic-implementation",
          "data_access": {
            "reads": [
              "INTERNAL"
            ],
            "writes": [
              "INTERNAL"
            ],
            "rationale": "Reads and writes task records (id, title, completed, priority, due_date, created_at) in a single-user PostgreSQL tasks table. All task fields are classified INTERNAL — no PII is stored (single-user app with no authentication or user-identifying fields). Reads occur on GET /tasks and GET /tasks/:id. Writes occur on POST /tasks (insert), PATCH /tasks/:id (update), and DELETE /tasks/:id (delete).",
            "side_effects": [
              {
                "type": "db_read",
                "classification": "INTERNAL",
                "fields": [
                  "id",
                  "title",
                  "completed",
                  "priority",
                  "due_date",
                  "created_at"
                ],
                "rationale": "SELECT queries against the tasks table to list all tasks or retrieve a single task by UUID."
              },
              {
                "type": "db_write",
                "classification": "INTERNAL",
                "fields": [
                  "id",
                  "title",
                  "completed",
                  "priority",
                  "due_date",
                  "created_at"
                ],
                "rationale": "INSERT, UPDATE, and DELETE operations against the tasks table for task lifecycle management."
              }
            ]
          },
          "authority": {
            "domains": [
              "tasks"
            ],
            "rationale": "This is the sole root component for the task management application. It is the single source of truth for the Task domain model, all task CRUD operations, validation rules, and REST endpoint contracts. The backend is authoritative — no client-side caching of state."
          }
        },
        "implementation_status": "contracted",
        "test_results": null
      }
    }
  },
  "engineering_decisions": [],
  "failure_history": [],
  "lessons_learned": [],
  "version": 1
}