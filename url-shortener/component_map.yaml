version: "1.0"
generated_by: constrain
session_id: "url_shortener_001"

components:
  - name: "url_shortener_api"
    description: "FastAPI service handling REST endpoints, business logic, and serving frontend static files"
    type: service
    port: 8000
    protocol: http
    data_access:
      reads: [PUBLIC]
      writes: [PUBLIC]
      rationale: "handles all link creation, retrieval, and analytics operations"
    authority:
      domains: ["short_code.*", "original_url.*", "hit_count.*", "created_at.*"]
      rationale: "single service owns all link data and business logic"
    dependencies: ["postgres_db"]
    constraints: ["C001", "C002", "C003", "C004", "C005", "C006", "C007", "C008"]

  - name: "frontend_ui"
    description: "Single-page HTML and vanilla JavaScript interface for URL submission and link management"
    type: service
    port: null
    protocol: http
    data_access:
      reads: [PUBLIC]
      writes: []
      rationale: "displays link data but does not directly write to storage"
    authority:
      domains: []
      rationale: null
    dependencies: ["url_shortener_api"]
    constraints: ["C006"]

  - name: "postgres_db"
    description: "PostgreSQL database storing links table with URL mappings and metadata"
    type: service
    port: 5432
    protocol: tcp
    data_access:
      reads: []
      writes: []
      rationale: "storage layer accessed only through url_shortener_api"
    authority:
      domains: []
      rationale: null
    dependencies: []
    constraints: ["C003", "C004", "C007", "C008"]

edges:
  - from: "url_shortener_api"
    to: "postgres_db"
    protocol: tcp
    description: "SQL queries for link CRUD operations and hit count updates"
  
  - from: "frontend_ui"
    to: "url_shortener_api"
    protocol: http
    description: "REST API calls for link creation, listing, and static file serving"